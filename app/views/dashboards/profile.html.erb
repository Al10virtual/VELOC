<div class="container">
  <h1>Mon compte</h1>
  <h2>Mes vélos disponibles à la location :</h2>
  <div class="row mt-5">
    <% if @bikes.present? %>
      <% @bikes.each do |bike| %>
        <div class="col-4">
          <%= link_to bike_path(bike) do %>
            <div class="card mb-4">
              <% if bike.photo.attached? %>
                <div class="card-top" style="background-image: url('<%= cl_image_path bike.photo.key %>')"></div>
              <% else %>
                <div class="card-top" style="background-image: url('<%= image_path 'placeholder_bike.jpg' %>')"></div>
              <% end %>
              <div class="card-bottom">
                <h5 class="card-title"><%= bike.brand %> <%= bike.model %></h5>
                <p class="card-text"><%= bike.bike_type.upcase%></p>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    <% else %>
      <p>Je n'ai pas mis de vélo en location pour le moment</p>
    <% end %>
  </div>
  <h2 id="mesreservations">Mes réservations :</h2>
  <div class="row mt-5">
    <% if @rentals.present? %>
      <% @rentals.each do |rental| %>
        <div class="col-4">
          <%= link_to bike_path(rental.bike) do %>
            <div class="card mb-4">
              <% if rental.bike.photo.attached? %>
                <div class="card-top" style="background-image: url('<%= cl_image_path rental.bike.photo.key %>')"></div>
              <% else %>
                <div class="card-top" style="background-image: url('<%= image_path 'placeholder_bike.jpg' %>')"></div>
              <% end %>
              <div class="card-bottom">
                <h5 class="card-title"><%= rental.bike.model %>  <%= rental.bike.brand %> de <%= rental.user.first_name %></h5>
                <p class="card-text">Ma location du <%= rental.start_date.strftime("%m/%d/%Y") %> au <%= rental.end_date.strftime("%m/%d/%Y") %></p>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    <% else %>
      <p>Je n'ai pas mis de vélo en location pour le moment</p>
    <% end %>
  </div>
    <h2>Demandes de location en attente :</h2>
  <div class="row mt-5">
    <% if @pending_rentals.present? %>
      <% @pending_rentals.each do |rental| %>
        <div class="col-4">
          <div class="card mb-4">
            <% if rental.bike.photo.attached? %>
              <div class="card-top" style="background-image: url('<%= cl_image_path rental.bike.photo.key %>')"></div>
            <% else %>
              <div class="card-top" style="background-image: url('<%= image_path 'placeholder_bike.jpg' %>')"></div>
            <% end %>
            <div class="card-bottom">
              <h5 class="card-title">Demande de <%= rental.user.first_name %></h5>
              <%= rental.status %>
              <p class="card-text">Location du <%= rental.start_date.strftime("%m/%d/%Y") %> au <%= rental.end_date.strftime("%m/%d/%Y") %></p>
              <%= button_to "Valider", accept_rental_path(rental), method: :patch, class: 'btn btn-success' %>
              <%= button_to "Refuser", deny_rental_path(rental), method: :patch, class: 'btn btn-danger' %>
            </div>
          </div>
        </div>
      <% end %>
    <% else %>
      <p>Il n'y a aucune demande de location en attente pour le moment.</p>
    <% end %>
  </div>
</div>
